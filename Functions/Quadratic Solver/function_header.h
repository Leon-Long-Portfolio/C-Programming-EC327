#ifndef FUNCTION_HEADER_H
#define FUNCTION_HEADER_H //define these functions

#include <cmath>
#include <iostream>

using namespace std;

double sqrt(double N, double tol);
double positiveX(int a, int b, int c, int tol);
double negative(int a, int b, int c, int tol);

#endif